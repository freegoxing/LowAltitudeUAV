"use client";
import { mockEvents } from "@/data/mock-events";import { useDashboardStore } from "@/stores/use-dashboard-store";import type { ConsoleTab } from "@/types/dashboard";import styles from "./workspace-layout.module.css";
const tabs:[ConsoleTab,string][]=[["events","事件时间线"],["agent","Agent 执行过程"],["rl","RL 训练状态"],["api","API 日志"]];export function BottomConsole(){const tab=useDashboardStore(s=>s.consoleTab);const set=useDashboardStore(s=>s.setConsoleTab);return <section className={styles.console}><div className={styles.tabs}>{tabs.map(([key,label])=><button className={`${styles.tab} ${tab===key?styles.activeTab:""}`} key={key} onClick={()=>set(key)}>{label}</button>)}</div>{tab==="events"?<div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>时间</th><th>类型</th><th>事件</th><th>关联对象</th><th>状态</th></tr></thead><tbody>{mockEvents.map(e=><tr key={e.id}><td>{e.time}</td><td>{e.type}</td><td>{e.message}</td><td>{e.relatedObject}</td><td>{e.status}</td></tr>)}</tbody></table></div>:<div className={styles.placeholder}>{tabs.find(([key])=>key===tab)?.[1]}将在动态数据阶段接入。</div>}</section>}
