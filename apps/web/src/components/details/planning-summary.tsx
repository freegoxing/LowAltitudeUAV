"use client";
import { PanelCard } from "@/components/ui/panel-card";import { SectionHeader } from "@/components/ui/section-header";import { StatusBadge } from "@/components/ui/status-badge";import { usePlanningStore } from "@/stores/use-planning-store";import styles from "./details.module.css";
export function PlanningSummary(){const r=usePlanningStore(s=>s.result);return <PanelCard><SectionHeader title="规划结果" meta={r.planId}/><div className={styles.body}><StatusBadge tone="green">规划已完成</StatusBadge><div className={styles.rows}><div className={styles.row}><span>关键节点</span><strong>{r.criticalNodeIds.length}</strong></div><div className={styles.row}><span>子图连通率</span><strong>{r.connectivityRate}%</strong></div><div className={styles.row}><span>平均时延</span><strong>{r.averageLatency} ms</strong></div><div className={styles.row}><span>平均丢包率</span><strong>{r.averagePacketLoss}%</strong></div><div className={styles.row}><span>规划耗时</span><strong>{(r.durationMs/1000).toFixed(2)} s</strong></div></div></div></PanelCard>}
